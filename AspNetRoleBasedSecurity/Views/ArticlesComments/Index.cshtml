@model IEnumerable<AspNetRoleBasedSecurity.Models.ArticlesComment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<div id="page-content" class="page-content">
    <table id="table_id" class="display">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Comments)
                </th>

                <th>
                    @Html.DisplayNameFor(model => model.ArticleId)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.User_n)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ThisDateTime)
                </th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Comments)
                    </td>

                    <td>
                        @Html.DisplayFor(modelItem => item.ArticleId)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.User_n)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ThisDateTime)
                    </td>
                    <td>
                        <label class="form-group" style="color:#45A163 !important; top:50px">
                            @Html.CheckBoxFor(modelItem => item.Com_mark,
                                                                    new { @id = item.CommentId, @class = "with-font" })
                            <label for="Maskasanswer">  </label>
                            <br />Mark
                        </label>
                    </td>
                </tr>
            }
        </tbody>
    </table>

</div>
<div>
    <a href="@Url.Action("Index", "Account" )"> Back </a>
</div>

<link rel="stylesheet" type="text/css" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
<script src="//code.jquery.com/jquery-1.11.1.min.js"></script>
<script src="http://code.jquery.com/jquery-latest.min.js" type="text/javascript"></script>
<script src="http://code.jquery.com/jquery-1.10.1.min.js"></script>
<script src="http://ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
<script type="text/javascript">
    $(function(){

        $(document).on("click","input.with-font", function (e) {
            var _this=$(this);
            var isChecked = _this.is(':checked');
            $.post("@Url.Action("Updatecheck", "ArticlesComments")?id="+_this.attr("id")+
                                           "&newValue="+isChecked,function(data){
                                               // do something with the response
                                           });

        });
    });

</script>


    <link rel="stylesheet" type="text/css" href="~/Content/DataTables/datatables.css">
    <script type="text/javascript" charset="utf8" src="~/Content/DataTables/datatables.js"></script>



    <script>

$(document).ready( function () {



    $("#table_id").DataTable();



} );

    </script>